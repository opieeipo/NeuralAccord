"""Complete developmental traces.

H4 turns on the distinction between what a system currently *is* and what an
external record says it *became*. That only works if the trace is complete
enough for an independent analyst to reconstruct the developmental route
without access to the system's operative state.

Traces are append-only and written outside Git; the repository keeps manifests
and checksums.
"""

__all__: list[str] = []
