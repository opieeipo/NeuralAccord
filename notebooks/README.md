# Notebooks

Exploratory analysis only. A notebook is never the source of a reported
result: anything that appears in a report is produced by a versioned YAML
declaration through `neural-accord`, with a seed manifest and a resolved
configuration.

Strip outputs before committing.
