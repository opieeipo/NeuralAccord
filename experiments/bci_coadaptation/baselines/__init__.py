"""The baseline registry.

There is no universal BCI baseline. Every dataset/task profile declares its
own, versions them, and runs them through the same split, preprocessing,
label-budget, and reporting pipeline as the method under test. The Neural
Accord co-adaptive method is never presumed superior.
"""

__all__: list[str] = []
